---
# Override target_users via -e (e.g. from `vercajk ansible ... --user`) to control who
# gets provisioned on this machine. Defaults to the current $USER when run outside the
# vercajk CLI (e.g. raw ansible-playbook).
target_users: ["{{ lookup('env', 'USER') }}"]

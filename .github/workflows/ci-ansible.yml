name: Ansible - Lint

on:
  push:
    paths: [ansible/**]
    branches: [main]
  pull_request:
    paths: [ansible/**]

jobs:
  lint:
    name: ansible-lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install ansible-core
        run: pip install ansible-core

      - name: Install collections
        run: ansible-galaxy collection install -r ansible/collections/requirements.yml

      - name: Run ansible-lint
        # Keep this in sync with the ansible-lint `rev` pinned in .pre-commit-config.yaml,
        # otherwise this job and pre-commit can disagree on what passes.
        uses: ansible/ansible-lint@v26.3.0
